# =============================================================================
# RECOVERLY - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: .env is in .gitignore - NEVER commit it to version control
# =============================================================================

# -----------------------------------------------------------------------------
# FIREBASE CONFIGURATION (REQUIRED NOW)
# -----------------------------------------------------------------------------
# Get these from: Firebase Console > Project Settings > General > Your Apps
# Select your web app or create one if you haven't yet
# All values prefixed with EXPO_PUBLIC_ are exposed to the React Native app

EXPO_PUBLIC_FIREBASE_API_KEY=your_api_key_here
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=your_auth_domain_here
EXPO_PUBLIC_FIREBASE_PROJECT_ID=your_project_id_here
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=your_storage_bucket_here
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here
EXPO_PUBLIC_FIREBASE_APP_ID=your_app_id_here
EXPO_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id_here

# -----------------------------------------------------------------------------
# OPENAI CONFIGURATION (REQUIRED NOW)
# -----------------------------------------------------------------------------
# Get from: https://platform.openai.com/api-keys
# This will be used in Firebase Cloud Functions to generate recovery protocols
# DO NOT prefix with EXPO_PUBLIC_ - this stays server-side only

OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# STRIPE CONFIGURATION (REQUIRED LATER - Phase 3)
# -----------------------------------------------------------------------------
# Get from: https://dashboard.stripe.com/apikeys
# Publishable key is safe to expose in the app (starts with pk_)
# Secret key must NEVER be exposed (starts with sk_) - stays server-side only

EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxx
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# EXERCISE VISUAL AIDS (REQUIRED FOR PHASE 2)
# -----------------------------------------------------------------------------
# YouTube Data API v3
# Get from: https://console.cloud.google.com/apis/credentials
# 1. Use your existing Firebase/Google Cloud project
# 2. Enable "YouTube Data API v3" in API Library
# 3. Create API key in Credentials
# 4. Restrict to YouTube Data API v3 only
# 5. Copy the API key (looks like: AIzaSyXXXXXXXXXXXXX)
# Free tier: 10,000 quota units/day (≈10,000 searches/day)
# This provides inline exercise tutorial videos (embedded with tap-to-play)

EXPO_PUBLIC_YOUTUBE_API_KEY=your_youtube_api_key_here
